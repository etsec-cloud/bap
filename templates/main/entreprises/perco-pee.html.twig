{% extends 'base.html.twig' %}

{% block title %}Assurance flotte véhicules{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href=" {{ asset('assets/css/assurance-auto.css') }}">{% endblock %}

{% block body %}
    
    <div id="autoContainer">

        <!-- Gestion des messages de succès/erreur -->
        {% for message in app.flashes('success') %}
            <div class="alert alert-success my-3">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger my-3">
                {{ message }}
            </div>
        {% endfor %}

        <h1 class="mt-5 mb-1">Votre assurance flotte véhicules</h1>
        <div id="autoContent">
            <div class="autoItem">
            <h1><strong>PEE</strong></h1><br>
                <p>
                   Le Plan d'épargne entreprise (PEE) permet aux salariés de se constituer une épargne dont l'effort repose en partie sur un abondement de l'entreprise.</p> <br>
            <h1><strong>PERCO</strong></h1><br>
            <p>
                   Le plan d'épargne pour la retraite collectif (Perco) vise pour sa part à permettre aux salariés d'épargner pour préparer leur retraite.<br>
                   Le Perco fonctionne sur le même principe que le PEE, l'épargne est constituée par un effort commun du salarié et de l'entreprise souscriptrice. La mise en place d'un PERCO est forcément postérieur à la mise en place d'un PEE.</p> <br>
                <p><strong>
                    Demandez votre devis comparatif sans engagement.
                    </strong></p><br>
                <p><strong>
                   Assurance & Cie s'occupe de toutes vos démarches pour résillier votre contrat actuel.
                    </strong></p>
            </div>
            <div class="autoItem">
                <h3 class="text-center">Demandez un devis dès maintenant !</h3>
                {{ form_start(assuranceForm) }}
                    {{ form_row(assuranceForm._token) }}
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            {{ form_row(assuranceForm.firstname) }}
                        </div>
                        <div class="form-group col-md-6">
                            {{ form_row(assuranceForm.lastname) }}
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col-md-8">
                            {{ form_row(assuranceForm.email) }}
                        </div>
                        <div class="form-group col-md-4">
                            {{ form_row(assuranceForm.tel) }}
                        </div>
                    </div>
                    <div class="form-group">
                        {{ form_row(assuranceForm.message) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(assuranceForm.documents) }}
                    </div>
                    <div class="text-center">   
                        {{ form_row(assuranceForm.submit) }}
                    </div>
                {{ form_end(assuranceForm) }}
            </div>
        </div>
    </div>

{% endblock %}