{% extends 'base.html.twig' %}

{% block title %}Assurance 2/3 roues{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href=" {{ asset('assets/css/assurance-auto.css') }}">{% endblock %}

{% block body %}
    
    <div class="autoContainer">

        <!-- Gestion des messages de succès/erreur -->
        {% for message in app.flashes('success') %}
            <div class="alert alert-success my-3">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger my-3">
                {{ message }}
            </div>
        {% endfor %}

        <h1 class="mb-1">Vos animaux de compagnie</h1>

        <div class="autoContent">
            <div class="autoItem">
                <p>
                    Votre animal fait partie de votre famille, à ce titre il mérite les meilleurs soins en cas de maladie ou d'hospitalisation. Nos offres vous permettent d'assurer votre compagnon à 4 pattes en préservant votre budget. Demandez votre devis gratuit.
                </p><br>
                <img src="{{ asset('assets/images/assurances/animaux-compagnie.png') }}" alt="Assurance-auto" width="140%"/>
            </div>
            <div class="autoForm">
                <h3 class="text-center">Demandez un devis dès maintenant&nbsp!</h3>
                {{ form_start(assuranceForm) }}
                    {{ form_row(assuranceForm._token) }}
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            {{ form_row(assuranceForm.firstname) }}
                        </div>
                        <div class="form-group col-md-6">
                            {{ form_row(assuranceForm.lastname) }}
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col-md-8">
                            {{ form_row(assuranceForm.email) }}
                        </div>
                        <div class="form-group col-md-4">
                            {{ form_row(assuranceForm.tel) }}
                        </div>
                    </div>
                    <div class="form-group">
                        {{ form_row(assuranceForm.message) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(assuranceForm.documents) }}
                    </div>
                    <div class="text-center">   
                        {{ form_row(assuranceForm.submit) }}
                    </div>
                {{ form_end(assuranceForm) }}
            </div>
        </div>
    </div>

{% endblock %}