{% extends 'base.html.twig' %}

{% block title %}Prévoyance familiale{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href=" {{ asset('assets/css/assurance-auto.css') }}">{% endblock %}

{% block body %}
    
    <div class="autoContainer">

        <!-- Gestion des messages de succès/erreur -->
        {% for message in app.flashes('success') %}
            <div class="alert alert-success my-3">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger my-3">
                {{ message }}
            </div>
        {% endfor %}

        <h1 class="mb-1">Votre prévoyance</h1>

        <div class="autoContent">
            <div class="autoItem">
                <p>A partir de 10€/mois - Toutes situtation - Contrat dans l'heure.</p>
                <p>
                    Protéger vos proches s'il vous arrive quelque chose, compléter vos revenus si vous ne pouvez plus travailler ou encore assurer la pérennité de votre entreprise...<br>
                    Nous vous accompagonons pour protéger ce qui compte le plus.<br>
                    Nous analysons votre situation familiale et professionnelle, vos assurance, pour mieux les compléter ou les optimiser et ainsi vous protéger au mieux.
                </p>
                <br>
                <img src="{{ asset('assets/images/assurances/prevoyance.png') }}" alt="Assurance-auto" width="140%"/>
            </div>
            <div class="autoForm">
                <h3 class="text-center">Demandez un devis dès maintenant&nbsp!</h3>
                {{ form_start(assuranceForm) }}
                    {{ form_row(assuranceForm._token) }}
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            {{ form_row(assuranceForm.firstname) }}
                        </div>
                        <div class="form-group col-md-6">
                            {{ form_row(assuranceForm.lastname) }}
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col-md-8">
                            {{ form_row(assuranceForm.email) }}
                        </div>
                        <div class="form-group col-md-4">
                            {{ form_row(assuranceForm.tel) }}
                        </div>
                    </div>
                    <div class="form-group">
                        {{ form_row(assuranceForm.message) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(assuranceForm.documents) }}
                    </div>
                    <div class="text-center">   
                        {{ form_row(assuranceForm.submit) }}
                    </div>
                {{ form_end(assuranceForm) }}
            </div>
        </div>
    </div>

{% endblock %}